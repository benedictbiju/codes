print("write the numbers below")

#float is used instead of int inorder to also add decimals 
x=float(input("number 1= "))
y=float(input("number 2= "))

#round used to round the decimals to a integer
z =round (x+y)

#f used to format the number (putting comas in big numbers)
print(f"{z:,}") 

